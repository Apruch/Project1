# Keamanan (SANGAT PENTING)
android.keystore

# Dependensi (Ukurannya sangat besar dan tidak perlu di-upload)
node_modules/

# File & Folder Build Android (Bawaan Bubblewrap)
android/
app/
gradle/
.gradle/
build/
gradlew
gradlew.bat
build.gradle
gradle.properties
settings.gradle
twa-manifest.json
manifest-checksum.txt

# Config lain
capacitor.config.json